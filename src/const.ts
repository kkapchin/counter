export enum Value {
    DEFAULT = 0,
    MAX = 5,
    MIN = 1,
}

export enum Button {
    INCREASE = 'inc',
    DECREASE = 'dec',
    RESET = '↻',
    SET = 'set',
}

export const COUNTER_STEP = 1;
